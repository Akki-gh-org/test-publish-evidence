name: Publish evidence item
on:
  push:
   branches: [ QA ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name : Step-1
        run: echo "hello from step 1"
        
      - uses: actions/checkout@v3

      - name: Publish Evidence Action
        uses: cloudbees-io-gha/publish-evidence-item@CBP-14461
        id: go-action
        with :
          cloudbees-url: "https://api.saas-qa.beescloud.com/"
          # cloudbees-pat: ${{ secrets.QA_PAT }}
          content: "Test QA evidence-OIDC"
